// Placeholder service worker for future custom logic.
// The generateSW strategy from vite-plugin-pwa produces the final service worker.
self.addEventListener('message', () => {
  // reserved for future messaging
})
